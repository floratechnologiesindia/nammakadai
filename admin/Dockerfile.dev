FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

ENV NODE_ENV=development
EXPOSE 5002

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5002"]

